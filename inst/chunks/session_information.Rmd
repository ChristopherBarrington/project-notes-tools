<!-- Session information, automatically generated. Files for environment/knitr chunks are respecified.  -->

## Session information

The below tabs show how the R environment was configured for this notebook.

{{< spoiler text="Show session information" open=false >}}

{{< callout "automated" >}}
The session information is automatically generated.
{{< /callout >}}

{{< tabs "session-information-tabset" >}}
{{< tab "R session" >}}
A session information file with package versions is available in [plain text](files/session_information.txt) and [yaml](files/session_information.yaml) formats.

The `conda` environment used to make this report was: ``r Sys.getenv('CONDA_PREFIX', unset='no conda environment prefix set!')``. 

```{r write_session_information, cache=FALSE, echo=FALSE}
options(width=60)
devtools::session_info() %T>%
  yaml::write_yaml(file=file.path(knitting_path, 'files', 'session_information.yaml')) %>%
  utils::capture.output(file=file.path(knitting_path, 'files', 'session_information.txt'))
```
{{< /tab >}}

{{< tab "Saved objects" >}}
These objects have been saved into the project data package and can be loaded into a new R session using `data()`.

```{r show_md5sums_for_saved_objects, dependson='record_saved_objects', cache=FALSE, echo=FALSE, eval=length(saved_objects)>0}
saved_objects %>%
  set_names() %>%
  plyr::ldply(.parallel=TRUE, function(x) get(x) %>% digest::digest()) %>%
  set_names(c('variable', 'md5 checksum')) %>%
  kable(format='html')
```
{{< /tab >}}

{{< tab "Packages" >}}
The following packages were loaded for this workbook:

```{r show_load_workbook_packages, cache=FALSE, echo=TRUE, eval=FALSE, ref.label='load_workbook_packages'}
```

<!-- For a complete list, with versions, see the files referenced above and the [bibliography](files/bibliography.bib) for citations. -->
{{< /tab >}}

{{< tab "Plot theme" >}}
The default `ggplot` theme was set accordingly:

```{r show_global_ggplot_theme, cache=FALSE, echo=TRUE, eval=FALSE, ref.label='define_global_ggplot_theme'}
```
{{< /tab >}}

{{< tab "Environment" >}}
```{r show_environment, cache=FALSE, echo=TRUE, eval=FALSE, code=readLines(system.file('chunks/environment.r', package='project.notes.tools'))}
```
{{< /tab >}}

{{< /tabs >}}
{{< /spoiler >}}
